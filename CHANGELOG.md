# Changelog

## Unreleased
- Added a built-in three-step Tutorial mode with highlighted placements and guided onboarding.
- Tutorial completion is now saved via `tutorialCompleted`, and the main menu CTA switches to replay state after the first completion.
- Retired unsupported build targets: `crazygames`, `poki`, `itchio`, `newgrounds`.
- Reduced the active build/dev matrix to `generic`, `yandex`, `vkplay`, and `rustore`.

## 0.1.0
- Initial playable build: core logic, UI screens, CrazyGames SDK hooks, saves, and tests.
